description = 'WorkTime'

allprojects {
    apply plugin: 'base'
    apply plugin: 'java'
    apply plugin: 'groovy'

    group = 'hu.unideb'

    repositories {
    	mavenCentral()
    }
	
    dependencies {
	}
}

